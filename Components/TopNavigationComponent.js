import { View, Text } from "react-native";
import { AppStyle } from "../AppStyle";

export default function TopNavigationComponent(params) {
  return (
    <View style={AppStyle.TopNavigationComponent}>
      <Text style={AppStyle.TopNavigationComponent_Title}>xtendly</Text>
    </View>
  );
}
